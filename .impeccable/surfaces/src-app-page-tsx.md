---
version: 1
slug: "src-app-page-tsx"
primary_target: "src/app/page.tsx"
related_targets: ["src/components/experiment-dashboard.tsx"]
---

Scope: the main experiment dashboard at `src/app/page.tsx` and its live client workspace. Mode: Operate.

Audience and job: multi-agent engineers and hackathon judges need to start one controlled experiment, see the three policies run, inspect explicit reaction evidence, compare measured outcomes, and launch one supported ablation.

Primary action: Run full experiment. Secondary navigation may open saved run and comparison routes after results exist.

Content and proof: live event records, measured intervals, explicit reaction edges, deterministic quality score, formula disclosure, run provenance, and the Production Incident #001 fixture. Never invent customers or external validation.

Chosen direction: The Incident Signal Board. The first viewport puts the product statement and run control above a wide ruled execution surface. The memorable moment is a pale signal trace crossing from an Evidence output to a Hypothesis reaction while the timer is still advancing.

Constraints: serious dark developer-tool UI, dense but scannable, one accent, small radius, no decorative AI motifs, no hidden failures, fixture mode labelled on every relevant result, mobile collapses structurally.

Unresolved: exact final token values will be captured after rendered iteration.
